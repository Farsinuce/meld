Flask>=3.0
waitress>=3.0   # serves the app instead of Flask's dev server: pure Python (freezes cleanly),
                # cross-platform, and happy staying up for days with multi-minute requests.
Pillow>=10.0   # elevation survey, the tray icon, and the icon build
pystray>=0.19  # tray icon. Optional: ArnisXL falls back to headless if it (or a working tray)
               # is missing — which is the normal case on GNOME, where the status area was
               # removed and an AppIndicator extension has to be installed by the user.
psutil>=5.9
osmium>=4.0    # optional: only needed to BAKE a local .pbf into the OSM data pack (offline OSM).
               # Everything else — incl. the OSM grid cache + live Overpass prefetch — runs without it.
shapely>=2.1.2
zstandard>=0.25.0   # the ONLY zstd binding Meld imports (src/export.py). The similarly named
                    # `zstd` package is NOT used: it was pinned here by mistake and its wheel is
                    # missing on some platforms, which failed the whole install.

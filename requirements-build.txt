# Build-time only - needed to package Meld, never to run it.
#   python -m pip install -r requirements.txt -r requirements-build.txt
#   python packaging/build.py --archive
-r requirements.txt
pyinstaller>=6.10
